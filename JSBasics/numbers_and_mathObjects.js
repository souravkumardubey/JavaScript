const num1 = 100;
const num2 = 50;

let val;

// Simple math with numbers

val = num1 + num2;
val = num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

// Math Object

val = Math.PI;
val = Math.E;
val = Math.round(2.8);
val = Math.round(2.4);
val = Math.ceil(2.4); // upper value
val = Math.floor(2.8);  // lower value
val = Math.abs(-5);
val = Math.sqrt(5);
val = Math.pow(5,2);
val = Math.min(5,2);
val = Math.max(5,2);

console.log(val);